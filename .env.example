# App settings
SECRET_KEY=your-secret-key-here-change-in-production

# Database - PostgreSQL (recommended) or SQLite
# PostgreSQL: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://aic:aic_dev_2024@localhost:5433/artemis
# SQLite (for local dev without PostgreSQL):
# DATABASE_URL=sqlite+aiosqlite:///./artemis.db

# Encryption key for provider API keys (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-encryption-key-here

# JWT settings
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
